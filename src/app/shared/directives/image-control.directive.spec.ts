import { ImageControlDirective } from './image-control.directive';

describe('ImageControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageControlDirective();
    expect(directive).toBeTruthy();
  });
});
